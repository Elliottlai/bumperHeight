<Window x:Class="TestALG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestALG"
        mc:Ignorable="d"
        Title="TestALG" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 影像路徑選取 -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="影像路徑：" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="ImagePathTextBox" IsReadOnly="True"
                     VerticalAlignment="Center" Padding="4,2"/>
            <Button Grid.Column="2" Content="瀏覽..." Width="70" Margin="6,0,0,0"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- JSON 參數路徑（自動組合） -->
        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="參數 JSON：" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="ParametersJsonPathTextBox" IsReadOnly="True"
                     VerticalAlignment="Center" Padding="4,2" Foreground="Gray"/>
        </Grid>

        <!-- 結果影像顯示 -->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Background="#1E1E1E">
            <Image x:Name="ResultImage" Stretch="Uniform"/>
        </Border>

        <!-- 操作按鈕 -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="RunButton" Content="執行"
                    Height="40" FontSize="16" Click="RunButton_Click"/>

            <Button Grid.Column="2" x:Name="ShowDefectButton" Content="顯示瑕疵結果"
                    Height="40" FontSize="16" Click="ShowDefectButton_Click"/>
        </Grid>
    </Grid>
</Window>
